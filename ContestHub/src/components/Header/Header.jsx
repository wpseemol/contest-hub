import NavBar from '../NavBar/NavBar';

const Header = () => {
    return (
        <header className="font-montserratFont ">
            <NavBar></NavBar>
        </header>
    );
};

export default Header;
